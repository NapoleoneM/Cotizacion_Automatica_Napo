# Credenciales del service account — nunca subir al repositorio
credentials/

# PIN de acceso y otras variables de entorno — solo en el servidor
.env

# Estado del día del panel de turnos (presencia y novedades) — solo servidor
datos/

# Plantilla generada localmente para importar a Sheets (no es código, no se sube)
Horarios_plantilla.xlsx

# Entornos virtuales
.venv/
venv/
env/

# Cache de Python
__pycache__/
*.pyc
*.pyo

# Archivos de sistema
.DS_Store
Thumbs.db
